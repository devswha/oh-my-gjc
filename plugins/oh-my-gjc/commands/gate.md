---
description: gate-briefing(승인 게이트 브리핑) 강제 적용을 켜거나 끈다. 인자 없거나 'on'이면 켜고, 'off'면 끈다.
argument-hint: "[on|off]"
---

사용자가 gate-briefing(승인 게이트 브리핑) 강제 적용을 토글했다.

입력 인자: $ARGUMENTS

처리 규칙:

- 인자가 비어 있거나 `on`이면 → **켜기**:
  지금부터 사용자가 `/oh-my-gjc:gate off`를 실행하기 전까지, 이번 세션에서
  **승인 게이트(pending-approval, 계획/실행 승인 요청)를 사용자에게 제시하는 모든 순간**에
  gate-briefing 스킬의 4부 브리핑을 함께 출력한다:
  1. 비전문가 번역 (일상어 5문장 이내)
  2. 승인의 경계 (지금 승인하는 것 / 승인하지 않는 것)
  3. 도메인-무지 체크리스트 (계획 원문 근거 필수, 없으면 "명시 없음 — 승인 전 확인 요망")
  4. 판정 (승인/보류/반려 추천 + 승인 전 질문 후보)
  - 계획 원문(pending-approval.md 등)과 critic/architect 판정을 실제로 읽고 작성한다.
  - "명시 없음" 행이 2개 이상이면 추천은 자동으로 "보류"다.
  - 승인/반려 실행은 절대 대행하지 않는다.
  - 출력은 `게이트 브리핑 모드: 켜짐` 한 줄만 확인으로 내보내고 끝낸다.

- 인자가 `off`이면 → **끄기**:
  강제 적용을 해제한다. (승인 게이트 자체는 평소처럼 동작한다.)
  출력은 `게이트 브리핑 모드: 꺼짐` 한 줄만 확인으로 내보내고 끝낸다.

- 그 외 인자면 → 사용법 한 줄만 안내한다: `/oh-my-gjc:gate [on|off]`.
