# v0.36.1 independent review — round 1

Reviewer: `kimi-code/k3`, isolated native GJC, read-only. Sources are the complete changed runtime and tests.

Review complete. Findings below are evidenced against the full source.

## Findings

**1. `pack_and_ask.py:1858` — HIGH — unscoped initial pill snapshot still carries the previous model's Pro label into a different model**

`select_model()` takes its pre-menu pill snapshot unscoped:

```python
pill_effort_before = _exact_effort_pill(page, want_l)   # line 1858, no require_model
```

The diff only scopes the *post-switch* re-read (line 1903). Reachable fail-open: composer pill is stale, e.g. `GPT-5.6 Sol\n최대`, but the open menu's checked model already matches `require_model='GPT-6 Astra'` (user had switched earlier, or pill DOM lagged). Then:

- line 1892 `_model_name_matches(before["model"], require_model)` is True → the new `select_listed_model` branch (which nulls and re-reads the pill scoped) is **skipped**;
- new picker menus have no effort radios, so `before["effort_checked"] is None` (line 1920);
- `pill_effort_before='최대'` from the **Sol** pill → `already_pill=True` (1922-1931);
- line 2010-2013: `effort_verified` True via `already_pill and pill_effort_before is not None`; `model_verified` True → `verified=True` → send proceeds (gate at 2899-2901 passes) with the previous model's Pro evidence attributed to Astra.

This is exactly the stale-pill GUI observation the change set out to fix, and the new test "rejects an old Sol pill that lingers after Astra becomes checked" only exercises the mismatch-then-switch path (mock starts with Sol active), so it never covers menu-checked-Astra + stale-Sol-pill. Fix: when `require_model` is a concrete name (not `None`/`"current"`), scope the 1858 snapshot, or re-snapshot scoped after the `"current"` resolution at 1863-1869.

**2. `pack_and_ask.py:1536-1545, 2008` — LOW — `_slider_effort_verified` pill read is unscoped post-switch**

After a successful `select_listed_model` switch, if the reopened menu yields no effort items and a `[role="slider"]` exists (1924-1937), `effort_verified` delegates to `_slider_effort_verified(page, want_l)`, whose `_exact_effort_pill(page, want_l)` call (1543) has no `require_model` scoping. A lingering previous-model Pro pill plus `aria-valuenow == aria-valuemax` satisfies it for `pro`. The slider drives the *current* model, so the at-maximum check partially mitigates, but the evidence pill can still belong to the old model. Same for the cosmetic `slider_label` at 2018. Thread `require_model` through.

## Verified clean

- `browser_launch_env` (850-870): preserves explicit `DISPLAY`/`WAYLAND_DISPLAY`; rejects symlinked socket dir, non-socket/non-owned entries, and 0/2+ sockets; read-only w.r.t. X auth — no `xhost`/cookie/login mutation anywhere in the diff. Tests cover owned/unowned/ambiguous/symlink/explicit cases.
- `wait_for_login_state` (2467-2474) + callers (961, 990): login wall returns `"no"` on first pass (terminal), `"unknown"` after deadline is handled fail-closed by all callers (1063-1069, 991, 2844-2848).
- `select_listed_model` (1828-1848): exact unique visible match, no click on ambiguity, checked-state re-verification; failures return `(False, None)` and the send gate at 2899-2901 aborts — no send on verification failure.
- Doc changes (SKILL.md, templates) match the implemented behavior.

Finding 1 is a fail-open on the central invariant of this release.

VERDICT: REQUEST_CHANGES
