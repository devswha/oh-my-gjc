# Independent cross-family review — v0.37.0

Candidate: `546df1f9799cec6ba3aa3ceb1d8bdb776654d42f`. Reviewer: `kimi-code/k3`, fresh native GJC process, read/search/find only, notifications/SDK/MCP disabled, no persisted session. Source snapshot was secret-scanned before egress. Process rc=0; elapsed 818.07 seconds. The following is the reviewer output. Its live-coverage observations reflect the supplied snapshot; subsequent actual browser/install/CI evidence is in the release verification document.

---

Review complete. I read the full snapshot diff index and the complete final source of every load-bearing changed file: `pack_and_ask.py`, `review_journal.py`, `install.sh`, `install-skill.sh`, `omg-autoupdate.sh`, `captions.py`, `fetch_chain.py` (changed regions + integration context), engine `__main__.py`, `insane_search.py`, all five templates, the changed SKILL.md/reference docs, `run_ci.py`, `run_sandbox_ci.py`, `record_provenance.py` diff, `test.yml`, pins/locks, the three verification docs, and all new/changed test suites.

**Requested outcomes — all verified in source:**

1. **Single attempted Pro send.** `journal.begin_send()` latches `attempted=True` and fsyncs `send_state="unknown"` inside `before_send()`, before `btn.click()`; there is no Enter/`keyboard.type` fallback, and every post-send exit/exception/timeout breaks the retry loop into harvest-only instructions. Click-transport errors, DOM loss after acceptance, quota, timeout, and echo pages are all terminal. `review_recovery_test.py` proves this with a composer/CDP harness that asserts `sends ≤ 1` across click errors, DOM errors, hydration gaps, restarts, and journal fsync failures (which latch *without* a click — fail-closed).
2. **Durable zero-send recovery.** The journal is schema-validated, 0600/0700, symlink/hardlink/duplicate-key/tamper-checked, fsynced and atomically published; recovery binds conversation + exact request hash + run marker + baseline + verified model + per-file source/pack SHA-256, and never guesses a turn from the newest answer. Response artifacts are exclusive-create 0600; existing/tampered files are never overwritten; crash-between-write-and-completion recovers correctly.
3. **Local-source install + native restoration.** `install.sh --local` validates the local catalog in isolated Python (duplicate keys, symlinks, name/version parity) before touching any registration, forces install from that checkout, and has no remote/unforced fallback; the updater forwards `--local` to both installer and payload. Native installs stage all files, snapshot previous bytes/modes, publish the binding last, restore on catchable failure, and recover SIGKILL/SIGTERM interruptions on the next invocation with conflict preservation of user edits — covered by genuine fault-injection tests (failing cp/mv, TERM/KILL, lock-probe concurrent installer, flock-absent `fcntl` fallback), plus a seccomp network-denied real-GJC lane.
4. **Extraction completeness + framed batch output.** PDF/JSON-LD caps now emit `truncation_reasons`, per-page/block failure counters, `coverage_uncertain`, and `extraction_complete=False|None`; `--body-json`/`--jsonl` emit one versioned record per input in input order (duplicates kept) with untrusted boundaries intact, per-URL SSRF guards, and no secret-bearing exception text.
5. **Public captions.** Explicit-only (`--captions` + exact `--caption-language`/`--caption-source`, no silent fallback), web-client-only yt-dlp subclass rejecting alternate clients before transport, auth/age-gate detection before yt-dlp's fallback loop, cookies/auth headers stripped, DNS-pinned GET/POST with redirect rejection, segmented/oversized VTT rejected rather than partially returned, and no HTML-grid fallback on caption failure. Integration tests run the real `YoutubeDL`/`YoutubeIE` with in-memory HTTP and blocked sockets.
6. **Read-only static setup.** The `/omg:setup` embedded check is stdlib-only, writes nothing, reports user+project scopes, binding damage, shadowing, and retired markers without executing providers/runtimes/network; snapshot-equality tests prove zero writes.
7. **Command expansion + honest CI.** All five templates carry `$ARGUMENTS`; the bubblewrap lane verifies exact expansion/quoting/literal-args against real GJC with shell-execution sentinels; CI is fully SHA/hash-pinned (actions, Python, Bun, 24 locked wheels, GJC/bwrap downloads) with skip-is-failure parsing, explicit named exclusions, and uploaded evidence.
8. **Boundaries kept.** 5 skills/5 commands unchanged; explicit-only `gpt-image`/`no-english`; public-URL/SSRF/proxy-strip guards intact; profile/lease binding proofs intact (`gpt_image_web` still resolves `cdp_binds_dedicated_profile`); repomix secretlint hermetically forced on; unowned user files/credentials/legacy state provably preserved.

**Non-blocking observations (not defects):** the legacy non-journal branch of `wait_for_turn_response` is now unreachable; `verify_packed_source`'s repomix line-number/CRLF rendering assumptions can't be re-confirmed offline but fail loudly at pack time and the release docs explicitly defer live re-verification; followup after `--delete-pack` fails closed (documented) via an uncaught `ValueError` rather than a clean message; live ChatGPT/YouTube/macOS coverage and NLP activation are explicitly documented as outstanding, not claimed.

No material user-affecting defects found; documented limitations are honestly scoped.

VERDICT: APPROVE
