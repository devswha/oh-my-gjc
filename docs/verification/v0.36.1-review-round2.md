# v0.36.1 independent review — round 2

Reviewer: `kimi-code/k3`, isolated native GJC, read-only. Sources are the complete changed runtime and tests.

Both findings verified against the actual updated source. Review below.

**Finding 1 (HIGH — stale Pro-pill cross-model attribution): CLOSED.**
- `select_model` now snapshots raw pills before opening the switcher (`pills_before = read_model_pills(page)`, pack_and_ask.py:1863) and only derives effort after model resolution: `pill_effort_before = _effort_from_pills(pills_before, want_l, require_model=require_model)` (:1876), which runs *after* the `"current"` → `selected_model_in_open_menu` resolution (:1868–1874). `_effort_from_pills` drops any pill whose first line doesn't match the required model (:1529), so a Sol pill can no longer satisfy Astra.
- The model-switch branch explicitly nulls the stale snapshot (`pill_effort_before = None`, :1899) and re-derives it post-switch with scoping (:1910).
- `inspect_session` mirrors this: pills captured pre-open (:993), effort derived only with `require_model=result["model"]` after the concrete menu read (:996–997); `selected_model_in_open_menu` only returns checked/row evidence, never a guess (:2036–2046).

**Finding 2 (LOW — unscoped slider evidence/label): CLOSED.**
`require_model` is threaded through `_drive_effort_slider` (:1498, :1509), `_slider_effort_verified` (:1541, :1548), the advanced-menu slider path (:1761, :1778), and both final label/verification call sites (:2015, :2025).

**Tests:** The new cases genuinely exercise the fixes — explicit and `current` no-switch stale-pill rejection with zero clicks, post-switch stale-pill rejection, fresh-evidence acceptance (`(True, 'GPT-6 Astra (최대)')` correctly built from the scoped pill fallback in `verified_effort_label`), and the slider binding (Sol pill rejected for Astra, accepted for Sol; aliases confirm `'최대' ∈ pro` candidates). Traced each against the code paths; all hold, including the fail-closed `clicked=None → (False, None)` exits.

**Regressions:** None found in the changed code. Fail-closed posture preserved (menu-row evidence required, no request-value fallback, :1774).

**Non-blocking observation (pre-existing, out of scope):** when `--require-model` is omitted entirely (arg default `None`, :2643 passed through at :2906), `pill_effort_before` is computed unscoped (:1876) and the `already_pill` path could pair the menu-read model name with a different model's lingering pill effort. Pills carry their own model line, so scoping to the menu-read model would be possible even in this mode. This mode performs no model verification by design and the behavior predates this fix (which strictly strengthened it), so it does not block this round — worth a follow-up if the no-require mode is ever used where the reported `Model (effort)` pair matters.

VERDICT: APPROVE
